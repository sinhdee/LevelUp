<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
</head>
<body>
    <h1>Profile for <%= user.username %></h1>
    <p>Email: <%= user.email %></p>
    
    <h2>COD Stats</h2>
    <ul>
        <% user.codStats.forEach(stat => { %>
            <li><a href="/codStats/<%= stat._id %>">View Stat: <%= stat.rank %></a></li>
        <% }) %>
    </ul>
</body>
</html>