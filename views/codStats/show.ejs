<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css"> <!-- Link to CSS -->
    <title>COD Stat Show Page</title>
    <style>
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f8f8;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1, h2 {
            color: #333;
        }

        .btn {
            display: inline-block;
            margin: 10px 5px;
            padding: 10px 20px;
            background-color: #ff6b6b;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }

        .btn:hover {
            background-color: #ff4040;
        }

        .btn.delete {
            background-color: #d9534f;
        }

        .btn.delete:hover {
            background-color: #c9302c;
        }

        section {
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>COD Stat Details</h1>
        <a href="/" class="btn">Home</a> <!-- Home button -->

        <!-- Display the COD stats -->
        <h2>Rank: <%= codStat.rank %></h2>   
        <h2>Kills: <%= codStat.kills %></h2>
        <h2>Deaths: <%= codStat.deaths %></h2>
        <h2>Assists: <%= codStat.assists %></h2>
        <h2>Result: <%= codStat.winLose ? 'Win' : 'Lose' %></h2> <!-- Display win/lose status -->

        <!-- Form to delete the stat -->
        <form method="POST" action="/codStats/<%= codStat._id %>?_method=DELETE" onsubmit="return confirm('Are you sure you want to delete this stat?');">
            <button type="submit" class="btn delete">Delete Stat</button>
        </form>

        <!-- Link to edit the stat -->
        <section>
            <a href="/codStats/<%= codStat._id %>/edit" class="btn">Edit Stats</a>
        </section>

        <!-- Link to go back to the stats overview -->
        <section>
            <a href="/codStats" class="btn">Back to All Stats</a>
        </section>
    </div>
</body>
</html>
